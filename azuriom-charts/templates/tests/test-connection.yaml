apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azuriom-charts.fullname" . }}-test-connection"
  labels:
    {{- include "azuriom-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azuriom-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
